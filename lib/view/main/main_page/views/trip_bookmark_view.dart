import 'package:flutter/material.dart';

class TripBookmarkView extends StatelessWidget {
  const TripBookmarkView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
